import PricingContent from './PricingContent';

export const metadata = {
  title: 'Pricing',
  description:
    'NorthBooks pricing — free plan with invoicing and HST tracking, Pro plan with T2 corporate tax, payroll, AI parsing, and 20+ export formats. Start free, no credit card needed.',
};

export default function PricingPage() {
  return <PricingContent />;
}
