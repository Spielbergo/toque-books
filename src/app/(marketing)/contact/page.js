import ContactContent from './ContactContent';

export const metadata = {
  title: 'Contact',
  description: 'Get in touch with the NorthBooks team — questions, bug reports, feature requests, or accountant partnerships.',
};

export default function ContactPage() {
  return <ContactContent />;
}
